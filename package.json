{
  "name": "hubspot-tracker-api",
  "version": "1.0.0",
  "description": "API to receive HubSpot tracking data from Chrome extension and save in MongoDB",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file .env src/server.js",
    "dev": "nodemon --env-file .env src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.19.2",
    "mongodb": "^6.8.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}